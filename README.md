# Swap Kids - Telegram Mini App

Telegram Mini App для обмена детскими вещами (одежда, игрушки, коляски, автокресла).

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка бота

**Важно:** В файле `main.py` замените токен бота:

```python
BOT_TOKEN = os.environ.get("BOT_TOKEN", "ВАШ_ТОКЕН_ЗДЕСЬ")
```

Или установите переменную окружения:
```bash
export BOT_TOKEN="ваш_токен_бота"
```

### 3. Запуск

```bash
python main.py
```

Приложение будет доступно на `http://localhost:8080`

## 📁 Структура проекта

```
swapkids_app/
├── main.py              # Основной сервер (aiogram + aiohttp)
├── requirements.txt     # Зависимости
├── templates/
│   └── index.html       # Интерфейс Mini App
└── swap_global.db       # SQLite база данных (создается автоматически)
```

## 🎨 Функции

### Главная
- 📱 Лента объявлений с фото
- 🔍 Поиск по названию и городу
- 🏷️ Фильтры по категориям
- ❤️ Добавление в избранное

### Категории
- 👕 Одежда
- 🧸 Игрушки
- 🚗 Коляски
- 💺 Автокресла
- 📦 Другое

### Премиум доступ
- 💎 Единоразовая оплата 500 Telegram Stars
- 🔓 Доступ ко всем контактам продавцов
- 👑 Бейдж Премиум в профиле

### Профиль
- 📦 Мои объявления
- 💎 Управление подпиской
- ⚙️ Настройки

## 🔗 Настройка в @BotFather

1. Отправьте `/mybots`
2. Выберите вашего бота
3. **Bot Settings** → **Menu Button** → **Configure menu button**
4. Укажите URL вашего приложения
5. Назовите кнопку, например: "🌍 Swap Kids"

## 🌐 Развертывание на Replit

1. Создайте новый Replit проект (Python)
2. Загрузите все файлы
3. В **Secrets** добавьте:
   - Key: `BOT_TOKEN`
   - Value: ваш токен бота
4. Нажмите **Run**
5. Replit автоматически предоставит URL

## 📝 API Endpoints

- `GET /` - Главная страница Mini App
- `GET /api/user` - Информация о пользователе
- `GET /api/items` - Список объявлений (с фильтрами)
- `POST /api/items` - Добавить объявление
- `DELETE /api/items` - Удалить объявление
- `GET /api/favorites` - Избранное
- `POST /api/favorites` - Добавить/удалить избранное
- `GET /api/contact` - Получить контакт (требует премиум)
- `GET /api/invoice` - Создать счет на оплату
- `GET /api/my-items` - Мои объявления

## 🛡️ Безопасность

- ✅ Токен бота только из переменных окружения
- ✅ Проверка владельца при удалении
- ✅ Проверка премиума перед показом контактов
- ✅ SQLite база данных локально

## 📱 Telegram WebApp Integration

Приложение полностью интегрировано с Telegram WebApp API:
- Автоматическое определение user_id
- Нативная кнопка "Назад"
- Оптимизировано для мобильных устройств

## 🤝 Поддержка

Если есть вопросы или проблемы, пишите в Telegram: @ваш_контакт